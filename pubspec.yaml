name: riverpod_pokedex_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  flutter_lints: ^2.0.0
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.0
  dio: ^5.5.0+1
  get_it: ^7.6.7
  skeletonizer: ^1.3.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
